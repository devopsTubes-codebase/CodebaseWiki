apiVersion: v1
kind: Service
metadata:
  name: codebase-wiki
  namespace: wiki-team
  labels:
    app: codebase-wiki
spec:
  type: ClusterIP
  selector:
    app: codebase-wiki
  ports:
    - name: http
      port: 80
      targetPort: http
